{% doc %}
  Renders a small all-caps label, typically placed above a heading.
  Reusable across hero, image-with-text, rich-text, etc.

  @example
  {% content_for 'block', type: 'eyebrow', id: 'eyebrow' %}
{% enddoc %}

{%- if block.settings.text != blank -%}
  <p
    class="eyebrow{% if block.settings.color_override %} eyebrow--color-override{% endif %}"
    {% if block.settings.color_override %}style="color: {{ block.settings.color }};"{% endif %}
    {{ block.shopify_attributes }}
  >
    {{ block.settings.text }}
  </p>
{%- endif -%}

{% stylesheet %}
  /* Eyebrow inherits color from the parent section. When the parent has
     a color scheme applied (.color-scheme-X), the eyebrow follows that
     scheme's text color by default — guaranteeing the merchant sees a
     visible change when they switch schemes. A section-level override
     (e.g. hero text_color_override) still wins via inheritance. */
  .eyebrow {
    font-family: var(--font-label-family, sans-serif);
    font-size: 0.6875rem;
    font-weight: 500;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    margin: 0 0 0.75rem;
    color: inherit;
    opacity: 0.85;
  }

  .eyebrow--color-override {
    opacity: 1;
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:blocks.eyebrow.name",
  "settings": [
    {
      "type": "text",
      "id": "text",
      "label": "t:blocks.eyebrow.text",
      "default": "Eyebrow"
    },
    {
      "type": "checkbox",
      "id": "color_override",
      "label": "t:blocks.eyebrow.color_override",
      "default": false
    },
    {
      "type": "color",
      "id": "color",
      "label": "t:blocks.eyebrow.color",
      "visible_if": "{{ block.settings.color_override == true }}"
    }
  ],
  "presets": [
    { "name": "t:blocks.eyebrow.name" }
  ]
}
{% endschema %}
